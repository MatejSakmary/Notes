## Push instruction
First increments (decrements) stack pointer by 4 bytes, and then places its input into 32bit space that just feed by moving stack pointer

## Pop instruction
Removes 4 byte element and places it into its specified input. Also decrements stack pointer

#intel #assembly #stack